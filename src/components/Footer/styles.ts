import { styled } from "@mui/material";

export const FooterContainer = styled('footer')`
    border-top: 2px solid #FFCDB2;
    background-color: #B5838D;
    padding: 6px;
    display: flex;
    flex-direction: column;
    align-items: center;

    p {
        font-size: 0.6rem;
    }
`